server:
  port: 8081
spring:
  application:
    name: "Authentication"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: false
      path: "/h2"
  profiles:
    include: null
  flyway:
    url: "jdbc:postgresql://crediya-db.cdaqau60wx86.us-east-1.rds.amazonaws.com:5432/crediya"
    user: "postgres"
    password: ":(^34tiE6eE,h]oU"
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validateMigrationNaming: true
    table: flyway_schema_history_auth
    default-schema: public
  datasource:
    url: "jdbc:postgresql://crediya-db.cdaqau60wx86.us-east-1.rds.amazonaws.com:5432/crediya"
    username: "postgres"
    password: ":(^34tiE6eE,h]oU"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"

adapters:
  postgres:
    database: "crediya"
    host: "crediya-db.cdaqau60wx86.us-east-1.rds.amazonaws.com"
    port: 5432
    username: "postgres"
    password: ":(^34tiE6eE,h]oU"
    schema: "public"
    pool:
      initial-size: 12
      max-size: 15
      max-idle-time: 10

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true


jwt:
  secret: "tuClaveSuperSecretaDeAlMenos32CaracteresAqui123!"
  expiration: 7200000